import sys

# num = int(input())
# stk = list(map(int,sys.stdin.readline().split()))
stk = [6,9,5,7,4]
result = [0]
prev = stk[0]
maximum = stk[0]
for x in stk[1:]:
    if prev < x:
        if maximum < x:
            
        
        else:

    else:

# for k in reversed(result):
#     j = str(j) + ' ' + str(k)
# print(j)